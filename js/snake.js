

function create_snake() {
  var snakeSize = 1;
  var snakeArray = []
  var score = 0
                                      // hash of coordinates within an array
  var snakeArray = snake_array.push({x: snakeSize, y:0})

}


// if snake hits prize then increment snakesize and add to array
